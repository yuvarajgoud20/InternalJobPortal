@model IEnumerable<InternalJobPortalMVC.Models.EmployeeSkill>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SkillID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SkillExperience)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SkillID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SkillExperience)
            </td>
            <td>
                    @Html.ActionLink("Edit", "Edit", new { empId = item.EmployeeID, skillId = item.SkillID }) |
                @Html.ActionLink("Details", "Details", new { empId = item.EmployeeID,skillId=item.SkillID }) |
                    @Html.ActionLink("Delete", "Delete", new { empId = item.EmployeeID, skillId = item.SkillID })|
                    @Html.ActionLink("ByEmployeeId", "ByEmployeeId", new { empId = item.EmployeeID })|
                    @Html.ActionLink("BySkillId", "BySkillId", new {  skillId = item.SkillID })
            </td>
        </tr>
}
    </tbody>
</table>
